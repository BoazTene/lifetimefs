# 🦀 lifetimefs

> A time-traveling filesystem written in Rust.

lifetimefs is a versioned FUSE filesystem that lets you access your data at any
point in its lifetime.

Read files as they existed yesterday, browse directories from last week, or
mount the entire filesystem at a specific timestamp — time becomes a first-class
dimension of the filesystem.
